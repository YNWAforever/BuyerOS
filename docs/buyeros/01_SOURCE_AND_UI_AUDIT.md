# Source and current UI audit

Revision v1. Audit window: 2026-09-14 UTC, desktop interactions recorded during this planning session (initial approval timestamp 18:40 UTC; subsequent wizard/pagination checks recorded in the ledger below). Exact source/metadata inspection plus authorized current desktop browser. No application, dependency, database or infrastructure writes.

## Evidence classes and limits

`SOURCE_VERIFIED` means inspected code at the recorded tree. `OBSERVED_UI` means rendered in this browser. `LOCAL_DEMO_VERIFIED` means a confirmed synthetic interaction changed local state. `LIVE_BACKEND_VERIFIED` is **not assigned to any capability**. `SPEC_ONLY` and `OWNER_REPORTED` are desired/historical context. `NOT RUN` means no fresh check was executed; `BLOCKED` names its prerequisite. Table API operations are all **PROPOSED** and are canonical `operationId` values in `contracts/openapi.proposed.yaml`; use that file to resolve exact method/path/schema.

## Current browser evidence ledger

Initial final URL: `https://fimmick-buyeros.laichiwillyjp.chatgpt.site/`. Browser viewport measured from the rendered document: 1348×936 CSS pixels, document scrollWidth 1348. Site public; no sign-in/access change or credential submission needed. The source and persistent banner confirmed all mutating controls tested were synthetic. No request to a paid service, external mailbox or live application backend was made.

| Check | Current observation | Evidence status / limitation |
|---|---|---|
| Desktop baseline | Fimmick sidebar, six navigation destinations including Settings; white/blue compact workspace, HarbourSense project, visible demo banner, run card/table/filter controls | OBSERVED_UI; [screenshot](evidence/desktop-discovery.jpg) |
| Run fixture | 24 unique, 26 raw, 2 duplicate mappings, 14 Match, 6 Review, sample USD6.60, 8 visible rows/page | OBSERVED_UI; actual dedup algorithms remain source simulation |
| DeltaGrid drawer | Overview/Evidence/Contacts/Activity tabs, missing purchasing facts, contrary evidence, three fictional sources with language/dates | LOCAL_DEMO_VERIFIED opening drawer and switching Evidence/Contacts |
| Acceptance | DeltaGrid changed Awaiting review→Accepted in drawer and later table | LOCAL_DEMO_VERIFIED; hardcoded actor, no server review |
| Contact quote | One selected/eligible, zero blocked; cap USD0.30; USD9.70 unreserved; quote ID/expiry and sample reservation shown | LOCAL_DEMO_VERIFIED; not current provider pricing |
| Contact confirmation | Confirm sample lookup produced Provider-marked valid DeltaGrid row and sidebar total USD6.90 | LOCAL_DEMO_VERIFIED; no actual contact identity validation |
| Draft workspace | Empty recipient queue initially; choosing accepted Rheinwerk and Prepare draft produced queue/editor/evidence review panes | LOCAL_DEMO_VERIFIED |
| Draft generation/approval | Generated sample, entered HarbourSense team as synthetic sender, checked demo policy, requested review and approved revision4 with hardcoded Willy attribution/time | LOCAL_DEMO_VERIFIED; no policy/legal authority inferred |
| Draft material edit | Subject edit produced Draft revision5; old approval text absent (count0), Send email isEnabled=false | LOCAL_DEMO_VERIFIED; browser state check, not server concurrency proof |
| Results/usage | Outcomes zero with manual/fictional/no-inbox disclosure; Usage showed total6.90, contact0.30, reserved0, remaining9.70; 6 accepted→1.15/accepted and 2 valid-contact accepted→3.45 | LOCAL_DEMO_VERIFIED tab switch; synthetic totals reconcile |
| Settings | Budgets, sample unit price, local save/reset, suppression row and four Not connected integration cards visible | OBSERVED_UI; no settings save/reset/suppression mutation executed |
| Locale | English→繁體中文 changed primary navigation/settings/disclosures; Research, Contact enrichment, Mailbox, CRM remain English; switched English back | LOCAL_DEMO_VERIFIED; whole-app language coverage NOT RUN |
| Search wizard | Navigated all four steps: manual offer, markets/types/languages, explicit profile confirmation, then discovery/contact budgets and demo scenario. Website analysis visibly Not connected and attachment Local only | LOCAL_DEMO_VERIFIED navigation/confirmation; file upload NOT RUN |
| Demo search | Run demo search returned completed sample Profile v2 with24 canonical,26 raw,14 Match/6 Review and fictional run cost2.40; accumulated sidebar9.30. Intermediate timer stages were not captured | LOCAL_DEMO_VERIFIED synthetic completion; durable/live progress NOT RUN |
| Pagination/selection/filter | Next page showed9–16 of24; Select current page checked visible rows; searching Rheinwerk returned1 row and0 selected | LOCAL_DEMO_VERIFIED; other filters/sort/selection combinations NOT RUN |
| Buyer Lists | Opened list view with European sensor partners,3-company membership badge, Create/Rename controls; retained Rheinwerk filter displayed1 row | OBSERVED_UI; create/rename/membership mutations NOT RUN |
| Deep links | Source inventory below describes actual branches and invalid-ID fallback | SOURCE_VERIFIED; direct navigation/reload matrix NOT RUN |
| Mobile, 390/768/1280/1440, 200% text zoom, full keyboard test | Current browser control surface exposes no viewport setter; did not alter Site or create another app/harness | NOT RUN; source CSS and prior developer handoff remain source/historical evidence only |
| Export download capture, WebMCP execution, reload persistence, full failure matrix | Not executed in current browser | NOT RUN; do not borrow historical pass results |
| Browser action timing | One Outreach click timed out after quote modal transition; fresh state inspection showed modal closed and later single navigation succeeded. Locale option attempt while menu closed had no match; reopened observed control | Tool/UI timing limitation; no evidence Site is broken or of suppression being applied |

No all-controls or mobile end-to-end pass is claimed. Current browser test coverage is deliberately narrower than the complete source/action mapping. `DEVELOPER_HANDOFF.md` test statements remain historical.

## Exact repository and stack inventory

### Identity


| Item | Evidence / conclusion |
|---|---|
| Inspected checkout | `/workspace/scratch/d089ea7d4026/buyeros-source` |
| Git HEAD | `76892126c86031bfe8e7ab517adba7f306040313` |
| Tree | `b4c6b5384ccfd6d0bdd5b4b92440427db83de7c1` |
| Working tree before planning | `git status --porcelain=v1` produced no entries: clean. |
| Local origin | Sites Git remote ending `/appgprj_6aa82285e5108191aac9c44c840c5efe.git`. No remote was changed. |
| Exact Site | `.openai/hosting.json` declares `project_id: appgprj_6aa82285e5108191aac9c44c840c5efe`; `app/layout.tsx` declares FIMMICK BuyerOS title and BuyerOS description. |
| Owner GitHub repository | Owner explicitly supplied `YNWAforever/buyerosgpt`. Coordinating audit verified GitHub main commit `b804ba8d1514a1049b7202c861278dd72c473a75` has the same tree `b4c6b5384ccfd6d0bdd5b4b92440427db83de7c1`. Commit IDs differ; source trees match. Canonical target updated to `YNWAforever/BuyerOS` (empty; exact import expected) per the BO-001 decision record. |
| Saved Site source | Coordinating audit verified version 1 references the local HEAD. This substantiates exact project identity; it does not prove backend functionality. |
| AGENTS | No `AGENTS.md` found in checkout, descendants, or applicable filesystem ancestors inspected. Do not infer that future execution checkouts have none: recheck before each task. |
| Existing project instructions | Read `README.md` and `DEVELOPER_HANDOFF.md` completely, plus manifests/configuration and Sites build/auth code. Preserve established runtime, pnpm lockfile, synthetic-data disclosures, and unrelated changes. |
| Conflicting instructions | README says `install:ci` uses npm/shared lockfile. Actual `package.json`, `pnpm-lock.yaml`, `scripts/install-pnpm.sh`, `scripts/pnpm-install.mjs` establish pnpm 11.25.0. Prefer executable source and frozen pnpm lock; propose correcting stale README only in an approved task. |

## 2. Actual runtime and infrastructure

| Concern | Exact existing evidence | Verified conclusion |
|---|---|---|
| Framework | `package.json` dependencies; `vite.config.ts` default `defineConfig`, `vinext()`; `scripts/run-framework.mjs` | **Vinext 1.0.0-beta.5**, Vite 8.0.13, React/React DOM 19.2.6, TypeScript 5.9.3. `next` 16.3.4 is present and its App Router APIs are used. This is neither a plain client SPA nor a verified standard Next/Vercel deployment. |
| Router | `app/layout.tsx:RootLayout`, `app/page.tsx:Page`, `app/[...slug]/page.tsx:Page`; `features/workspace.tsx:Workspace` uses `usePathname`, `useRouter` from `next/navigation` | Root layout mounts the persistent client Workspace and does not render `children`; both page components return null. Workspace branches on path strings and performs `router.push`. Preserve the user journey and route behavior while adapting data. Do not add assumed page components underneath an ignored `children` slot. |
| Packages | `package.json:packageManager`, `pnpm-lock.yaml`, `pnpm-workspace.yaml` | pnpm **11.25.0**; Node minimum **22.13.0**. Existing dependency policy includes minimumReleaseAge and strictDepBuilds. No install was run. |
| UI | `app/globals.css`; `features/ui.tsx:Button,Pick,Pill,Field`; `components/ui/*` | Tailwind 4.2.1, Radix-backed primitives, shadcn-related packages, Lucide icons, Sonner, custom product CSS. Preserve coherent compact workspace visual language. |
| Runtime build | `scripts/run-framework.mjs`, `scripts/execution-profile.mjs:readExecutionProfile`, `scripts/build-verified.sh` | Managed Linux uses Vite/dev and verified build wrapper; portable uses Vinext CLI. Ignored execution profile selects mode; clean checkouts default portable. Runtime commands are not equivalent to `next build`. |
| Deployment target | `vite.config.ts:localBindingConfig`, Cloudflare plugin, `package.json:start` | Cloudflare Worker output uses `vinext/server/fetch-handler` and Wrangler. Start references `dist/server/wrangler.json`. No tracked Vercel deployment configuration, Docker deployment, CI workflow, or Python service exists. Vercel compatibility needs a separate verified portability spike, not an assumption. |
| D1 / R2 | `.openai/hosting.json`, `cloudflare-env.d.ts`, `db/index.ts:getDb` | Both declarations are null; optional DB/BUCKET types exist. `getDb()` throws if DB binding absent. No BuyerOS database integration. |
| Schema / migrations | `db/schema.ts`, `drizzle.config.ts`, `drizzle/meta/_journal.json` | Empty schema; SQLite Drizzle scaffold and empty migration journal. No domain tables and no executed migrations evidenced. Keep scaffold unused if PostgreSQL/Alembic becomes the sole domain owner; do not establish a competing D1 domain backend. |
| API routes | Only `examples/d1/app/api/notes/route.ts:GET,POST`; `tsconfig.json` excludes examples | Example notes API is outside app routing; no actual BuyerOS HTTP API route was found. |
| Auth | `app/chatgpt-auth.ts:getChatGPTUser,requireChatGPTUser,chatGPTSignInPath,chatGPTSignOutPath` | Optional Sites dispatch identity helpers exist but are not imported by BuyerOS UI or routes. Hardcoded Willy Lai labels are demo identity, not authenticated author attribution. No server membership/role authorization exists. |
| Local auth plumbing | `build/sites-vite-plugin.ts:sites`, `vite.config.ts` | Portable loopback-only local identity simulation exists, strips incoming identity headers, and is disabled in managed Linux. This is development scaffolding, not API authentication. FastAPI must not trust browser-submitted `oai-authenticated-user-*` headers. |
| Backend / jobs | `services/mock-client.ts`, `services/run-engine.ts`, Workspace timer | All domain operations are synchronous client transformations except timers/local browser APIs. No FastAPI, LangGraph, task queue, worker host, provider SDK, or model call is present. |
| Live adapter | `services/http-client.ts` reexports `httpClient` from `services/mock-client.ts`; its `request` throws structured `NOT_CONFIGURED` | Useful fail-closed behavior; not a typed implemented client. `BuyerDiscoveryClient` is only a proposed interface and the UI does not use it. |

## 3. Exact files and symbols to preserve or adapt

| Existing path | Symbols / control locations | Scope |
|---|---|---|
| `features/workspace.tsx` | default `Workspace`; `nav`, `stages`; `update`, `reviewed`, `action`, `closeModal`, `clear`, `startRun`, `editDraft`, `buyerTable` | Owns virtually all screen state, routes, modals, data mutations, metrics, local persistence, demo timer and read-only WebMCP registration. Long compressed source lines make symbol-level references more useful than guessed file splitting. |
| `features/discovery/wizard.tsx` | `Offer`, `sample`, `Wizard`; nested `set`, `next` | Four-step offer/profile/search form; Zod checks; local file selection; profile confirmation. |
| `features/buyers/detail.tsx` | `BuyerDetail` | Overview/Evidence/Contacts/Activity tabs; fit, missing facts, contrary evidence; owner/note edits; buyer/list/lookup/draft actions. |
| `services/contracts.ts` | `RecordBase`, `Company`, `Evidence`, `Project`, `ICPVersion`, `SearchRun`, `BuyerList`, `EnrichmentQuote`, `OutreachDraft`, `CostEvent`, `OutcomeEvent`, `Store`, `BuyerDiscoveryClient` | Current demo types plus explicitly proposed network boundary. Separate states already partly exist, but are shallow and mutable. |
| `services/mock-client.ts` | `money`, `usage`, `block`, `quote`, `confirm`, `csv`, `download`, `httpClient` | Synthetic eligibility/quotes/settlement, usage math, export safety, explicit unconfigured adapter. |
| `services/run-engine.ts` | `advanceRun`, `cancelRun`, `retryRun` | Deterministic 700ms-driven sample run simulation; stage cost deduplication by event ID. Keep only in demo adapter. |
| `data/demo/fixtures.ts` | `base`, `seed`, `rawCandidates` | 24 canonical fictional companies, 14/6/4 fit buckets, 26 raw mappings, sample initial cost USD 6.60, five accepted buyers. `rawCandidates` is not used by actual ingestion code; counts do not prove a deduplication implementation. |
| `locales/index.ts`, `locales/context.tsx` | `zh`, `LocaleContext`, `useT` | Source-string dictionary and English fallback; partial zh-HK coverage. |
| `components/ui/sidebar.tsx`, `hooks/use-mobile.ts` | `SidebarProvider`, `Sidebar`, `SidebarTrigger`, `useIsMobile` | Native UI breakpoint 768px; Workspace auto-open breakpoint 1024px. Preserve and test both tablet/mobile cases. |
| `tests/domain-checks.mjs` | `url`, `test`, `reserved`, `running`; 11 checks | Demo regression source. No database concurrency, API, tenant, browser, or provider integration coverage. |
| `tests/responsive-harness.html` | Four same-origin iframe widths | Existing 390/768/1280/1440 reference harness; not a test runner and not publicly served by default. |
| `lib/id.ts` | `uid` | Explicitly synthetic IDs with UUID fallback; must not become authentication tokens or authoritative server identity. |

## 4. Route and navigation behavior

| Route / state | Source-verified behavior | Required preservation / correction |
|---|---|---|
| `/` and `/app/discover` | Find Buyers with current active run. `/` is not a login page. | Preserve useful entry. Live authentication must occur without replacing the product shell. |
| `/app` | Overview cards derive pending buyers, accepted needing contacts, drafts awaiting review; one hardcoded project. | Preserve next-action navigation; scope aggregate to authorized workspace/project. |
| `/app/discover/new` | Four-step Wizard, state kept in persistent Workspace; wizard-local step/file state resets on unmount. | Persist server offer/profile drafts explicitly; warn about unsaved edits; accepted profile immutable. |
| `/app/discover/:runId` | Existing IDs choose activeRun. Unknown ID silently falls back to current run; after reload session-only IDs are missing. | Return an explicit authorized missing-run state; load by ID; do not display sample data for a missing live run. |
| `/app/buyers/:buyerId` | Existing fixture buyer uses full-page dossier; unknown buyer has explicit Buyer not found. | Preserve; tenant/project checks server-side; URL should recover after reload. |
| Buyer drawer / evidence dialog | `buyerId` and `source` local state, no query serialization. Closing drawer retains page context. | Preserve drawer and full-page alternatives; optionally add scoped URL state as a proposed enhancement. |
| `/app/lists` and `/app/lists/:listId` | List strip, create/rename, current-list table. Unknown list produces empty rows and still exposes rename flow. | Add missing/unauthorized states. Preserve membership-only removal and evidence. |
| `/app/outreach` | Local recipient queue chooses `draftId` or first draft. Any deeper path prefix renders same screen; ID is not parsed. | Add an approved draft deep-link convention; preserve editor/requirements/queue layout. |
| `/app/results` | Local Outcomes/Usage tab; sidebar usage sets tab before route push. | Preserve manually recorded outcomes and separate usage; URL tab state is proposed. |
| `/app/settings` | Device prefs, demo budgets, suppression, integration requirements. | Live permissions determine visible controls; same UX concepts remain. |
| Other paths | Catch-all route + negative screen booleans render Find Buyers by default. | Explicit supported-route parsing and not-found view; no silent fallback to sample run. |
| Filters and project | Filter state, preset, selected IDs, locale and current project are not query parameters. Project selector only offers HarbourSense/sample or new search. “Create project” opens wizard, not a persisted project create. | Real project selection must load and invalidate every scoped cache, selection, quote and draft context. Do not call current selector multi-project support. |


## Action → API → entity → guard → cost → failure → test → task

All 32 major action groups below preserve their existing UI affordance while replacing source-listed local behavior. Every `Workspace` symbol is in `features/workspace.tsx`; `Wizard`/`Offer` are in `features/discovery/wizard.tsx`; `BuyerDetail` is in `features/buyers/detail.tsx`; `quote/confirm/block/usage/csv/download/httpClient` are in `services/mock-client.ts`; run functions are in `services/run-engine.ts`. These are exact inspected files, not guessed `src/` paths. Source API column in the appendix is a discovery sketch; this table's operation IDs and the proposed OpenAPI own final naming. No new implementation path is claimed to exist.

| Requirement / UI action | Exact existing component / route and current source behavior | Current evidence | PROPOSED API operations | Entity / transition | Server permission / policy | Cost | Failure / concurrency | Acceptance obligation / task |
|---|---|---|---|---|---|---|---|---|
| REQ-IDENTITY / UI-01 | Shell and project picker; `Workspace:nav`, `modal==='project'`, `/app`<br>One hardcoded project; persistent React state, no tenant identity | SOURCE_VERIFIED; browser subset above | `listWorkspaces`, `listProjects`, `getProject` | WorkspaceMembership, Project; selected context changes | Verified subject + membership; never client workspace ID alone | Free read | Expired identity, forbidden project, stale requests returning after switch | Switching projects clears old data/selection and forbids cross-tenant IDs; TEST-BO-004-01; BO-004, BO-007 |
| REQ-PROFILE / UI-02 | Create/edit offer; `Wizard`, `Offer`, `Workspace:offer`<br>Local fields; Save demo draft only returns to results and retains memory | SOURCE_VERIFIED; wizard navigation observed, save/reload NOT RUN | `createProject`, `updateProject` | Offer draft revisions; explicit save | Project editor | Manual entry free; later URL analysis independently gated | Validation, stale edit, unsaved navigation, idempotent create | Reload preserves approved saved offer; stale patch 409; TEST-BO-007-01; BO-007 |
| REQ-PROFILE / UI-03 | Offer file; `Wizard` file input<br>Extension/size ≤5MB; TXT/MD read locally; PDF merely selected; file not part of run input | SOURCE_VERIFIED; browser execution NOT RUN | `uploadOfferDocument`, `ingestOfferUrl`, `getOfferDocument` | OfferDocument / extraction status; never silently accepted as analyzed | Upload permission, allowed MIME/size, purpose/retention | Parsing paid if LLM, reserve before use | Content sniffing, malformed/encrypted PDF, hostile text, SSRF for URL | No result fabricated; extracted text shown for review; unimplemented upload remains labeled; TEST-BO-007-01; BO-007, BO-012 |
| REQ-PROFILE / UI-04 | Target markets/types/languages; `Wizard` step 2<br>Local field controls; fixed German/Dutch query preview | SOURCE_VERIFIED; wizard navigation and sample run observed | `saveICPVersion`, `startRun` | ProfileVersion / query plan; draft until approved | Editor; model outputs untrusted suggestions | Preview model budget if used | Unsupported language/market; invalid profile; provider error | Target edits invalidate confirmation; original/source language retained; TEST-BO-007-01; BO-007, BO-013 |
| REQ-PROFILE / UI-05 | Confirm buyer requirements; `Wizard:next`, checkbox<br>Boolean confirmed; edits reset it; no saved immutable version | SOURCE_VERIFIED; browser execution NOT RUN | `approveICPVersion` | ProfileVersion draft → approved | Authorized reviewer | Free | Stale revision, double click, subsequent material edit | Approved hash persists; changed requirement requires new approval; TEST-BO-007-01; BO-007 |
| REQ-DISCOVERY / UI-06 | Start run; `Workspace:startRun`<br>Fixture equality check then seeded run; custom fields limited; no model/research | SOURCE_VERIFIED; browser execution NOT RUN | `startRun` | SearchRun queued → running; immutable profile/run link | Research permission; allowed targets/providers | Server discovery cap and reservation before paid steps | Double submit, insufficient budget, unapproved profile, queue transaction failure | One run/outbox record per key; forbidden/over-budget zero provider calls; TEST-BO-013-01; BO-013, BO-016 |
| REQ-EVENTS / UI-07 | Progress and partial results; Workspace 700ms effect, `advanceRun`<br>Timer slices global fixture to 8/16/24; repeated stage IDs dedup demo costs | SOURCE_VERIFIED; browser subset above | `getRun`, `getRunEvents` | RunEvent, RunCompany, JobAttempt; partial → completed | Authorized project read | Actual ledger from worker events | Worker crash, event replay/gap, stale UI, unknown provider status | Refresh resumes same run; no duplicate company/cost on event replay; TEST-BO-011-01; BO-011, BO-016 |
| REQ-EVENTS / UI-08 | Cancel/retry; `cancelRun`,`retryRun`, job panel<br>Stops timer or resumes same run with retry sample; retains count | SOURCE_VERIFIED; browser execution NOT RUN | `cancelRun`, `retryRun` | running → cancel_requested → cancelled; retry attempts explicit | Run operator | Keep in-flight holds until provider outcome known | Cancellation races commit/provider response; retry only safe/idempotent steps | In-flight accepted submission not auto-refunded/resubmitted; partial evidence retained; TEST-BO-016-01; BO-016, BO-020 |
| REQ-EVENTS / UI-09 | Run history; `Workspace` `s.runs.map`<br>Session runs and profile counters, route push; missing run fallback | SOURCE_VERIFIED; browser execution NOT RUN | `listRuns`, `getRun` | SearchRun history, immutable assessment snapshot | Read membership | Free | Pagination/404/forbidden; repeated IDs; historical profile | Old run results remain tied to old assessment/profile after new run; TEST-BO-016-01; BO-016 |
| REQ-BUYERS / UI-10 | Search, filters, sort; `rows` useMemo, `filterFields`, filter sheet<br>Client substring; market/type/fit/review/contact/source filters; “Old” hides all, “Recent” same as any | SOURCE_VERIFIED; browser execution NOT RUN | `listBuyers` | Scoped company/assessment/review/contact projection | Read scope, source access | Free bounded DB read | Unsupported filter; stale response; recency semantics/timezone | Dataset fixtures distinguish recent/old; stable ordering; all filter fields tested; TEST-BO-008-01; BO-008 |
| REQ-BUYERS / UI-11 | Page size and pagination; `pageRows`, `buyerTable`<br>Offset slice 8/12/24; clamps shown page | SOURCE_VERIFIED; browser execution NOT RUN | `createBuyerSnapshot`, `listBuyers` | BuyerPage, selection snapshot | Read scope | Free bounded read | Dataset changes between pages; duplicate/missing rows; expired snapshot | Navigate multiple pages without duplicate IDs under stable snapshot; TEST-BO-008-01; BO-008 |
| REQ-BUYERS / UI-12 | Current-page and all-filtered selection; `selected`,`Sel`, bulkbar<br>Client IDs; explicit all filtered; stale selections can remain after mutation | SOURCE_VERIFIED; browser execution NOT RUN | `createBuyerSnapshot`, `reviewBuyers` | Selection scope binds project/run/filter version | Validate each target | Lookup budget only on confirmed eligible set | Selection membership changes, partial failures, 10k ID abuse | All-filtered count excludes contacts/duplicates; changed scope invalidates selection; TEST-BO-008-01; BO-008 |
| REQ-BUYERS / UI-13 | Save/apply filter preset; `Preset`<br>Single session snapshot excludes search text/sort | SOURCE_VERIFIED; browser execution NOT RUN | `saveFilterPreset`, `listFilterPresets` | FilterPreset version | User-owned or workspace editor for shared preset | Free | Removed filter fields, stale preset, cross-project use | Applying preset reproduces documented fields; absent server feature honestly labeled; TEST-BO-008-01; BO-008, BO-025 |
| REQ-BUYERS / UI-14 | Open buyer/drawer/full-page; `BuyerDetail`,`B`,`fullBuyer`<br>Fixture detail with four tabs | SOURCE_VERIFIED; browser subset above | `getBuyer`, `listBuyerEvidence` | Company identity plus immutable assessments | Read membership; purpose-restricted contact access | Free; no implicit enrichment | Missing buyer, revoked evidence/contact, loading/error | Drawer/full-page show same buyer; failed live fetch shows error and zero fixtures; TEST-BO-008-01; BO-008, BO-014 |
| REQ-EVIDENCE / UI-15 | Evidence source panel; `Source`, `BuyerDetail` requirement/card<br>Authored excerpts, sample IDs/date/language; no actual URLs | SOURCE_VERIFIED; browser subset above | `getEvidence` | Evidence, SourceFetch, assessment-evidence links | Source visibility / retention restrictions | Retrieval paid only as explicit job | Unsafe URL, changed content, unavailable source, prompt injection | Claim links to exact evidence/hash and observed time; contradictions and unknowns remain visible; TEST-BO-014-01; BO-014 |
| REQ-BUYERS / UI-16 | Owner/note; `BuyerDetail` → `Workspace:update`<br>Every keystroke patches local Company | SOURCE_VERIFIED; browser execution NOT RUN | `updateBuyer` | Assignment, Note, audit event | Member editor; allowed owner membership | Free | Concurrent edit, deleted owner, length limits, XSS | Conflict preserves both user context and server version; no unsafe HTML; TEST-BO-008-01; BO-008 |
| REQ-BUYERS / UI-17 | Accept / reject single or bulk; `action`,`reviewed`<br>Accept fixed reason/actor; reject reason dialog; draft revisions reset | SOURCE_VERIFIED; browser subset above | `reviewBuyers` | HumanReview separate from AI fit; pending → accepted/rejected | Human review role; exact assessment context | Free | Stale assessment, mixed permissions, retry, duplicate audit | Accept never changes AI fit/contact validity; rejection invalidates relevant approval; TEST-BO-008-01; BO-008 |
| REQ-BUYERS / UI-18 | Create/rename/save/remove list; `Workspace` list modal and list strip<br>Local list; Set membership prevents duplicate; remove keeps company | SOURCE_VERIFIED; browser execution NOT RUN | `createBuyerList`, `renameBuyerList`, `changeListMemberships` | BuyerList, ListMembership; membership only | List editor + target company access | Free | Unique membership conflict, stale rename, unknown list, partial batch | Duplicate save one membership; removing leaves evidence intact; TEST-BO-008-01; BO-008 |
| REQ-EXPORT / UI-19 | CSV export; `csv`,`download`, buyerTable<br>Blob CSV; formula prefix neutralization; selected or filtered rows; `data_mode=demo` | SOURCE_VERIFIED; browser execution NOT RUN | `exportBuyers`, `getExport`, `downloadExport` | ExportJob or bounded export snapshot | Export permission; contact PII policy separately scoped | DB/export cost cap if large; no enrichment implicit | Stale selection, injection, stream/download failure, unauthorized fields | Quoted commas/newlines/formulas safe; exports scoped and correctly mode-labeled; TEST-BO-023-01; BO-023 |
| REQ-QUOTE / UI-20 | Lookup quote; `action('lookup')`, `quote`,`block`<br>Gates accepted/Match/not suppressed/Allowed/not researched; immediately creates client Reserved quote at configurable USD 0.30 | SOURCE_VERIFIED; browser subset above | `quoteLookup`, `getLookupQuote` | Quote immutable price/eligibility/policy snapshot; proposed issued not provider job | Contact research purpose-specific decision; suppression recheck | Quote estimate is not paid provider action; reserve atomically at confirm | Expired eligibility/pricing, duplicate quotes, provider unavailable | Quote returns eligible and blocked reasons; no charged lookup before confirm; TEST-BO-017-01; BO-017 |
| REQ-CONFIRM / UI-21 | Confirm lookup; quote modal → `confirm`<br>Recheck some gates, synchronous fixture status, one cost event by quote ID | SOURCE_VERIFIED; browser subset above | `confirmLookup` | Reservation + EnrichmentJob + outbox atomically committed | Revalidate subject, buyer acceptance/fit, policy, suppression | Fixed-precision NUMERIC(20,6) / Decimal budget locked transactionally | Same key/different body, concurrent caps, expiry, timeout after submit, uncertain result | Concurrent confirms create one job; unknown submission retains hold and reconciles; TEST-BO-018-01; BO-018 |
| REQ-RECONCILE / UI-22 | Cancel lookup / close quote; `closeModal`<br>Any local Reserved quote cancelled; expiry excluded from usage | SOURCE_VERIFIED; browser execution NOT RUN | `cancelLookupQuote`, `cancelEnrichmentJob` | Quote cancellation separate from job/reservation states | Operator permission | Release only unsubmitted hold or verified no-charge outcome | Cancellation during HTTP call or webhook; provider continues | UI close never proves provider cancelled; final charge reconciled exactly once; TEST-BO-017-01; BO-017, BO-020 |
| REQ-PROVIDER / UI-23 | Contact status in drawer; `BuyerDetail` Contacts<br>Status from fixture index; sample address derived from company name; no actual ContactPoint collection | SOURCE_VERIFIED; browser subset above | `getEnrichmentJob`, `getBuyer` | ContactPoint validity independent of suppression/policy | Authorized purpose to view contact data | No automatic new lookup on read | Catch-all/unavailable/stale validity; provider response mismatch | Contact provenance/check time shown; validity never confers outreach permission; TEST-BO-019-01; BO-019, BO-020 |
| REQ-POLICY / UI-24 | Add/remove suppression; settings modal → `update`<br>Requires reason; overwrites contact to Suppressed, removal resets Not researched | SOURCE_VERIFIED; browser execution NOT RUN | `createSuppression`, `removeSuppression` | Suppression rule independent; contact remains immutable history | Policy administrator; opt-out removal controlled | Free; in-flight cost still reconciled | Race with quote/approval; replay; contact retained after suppression | Suppression immediately blocks lookup/approval while prior contact validity/history remains; TEST-BO-009-01; BO-009 |
| REQ-DRAFT / UI-25 | Prepare draft; `action('draft')`<br>Accepted unsuppressed only; recipient derived .example; existing draft reused | SOURCE_VERIFIED; browser subset above | `generateDraft` | OutreachDraft revision 1; delivery always not_connected in pilot | Draft permission, required contact purpose, current evidence | Template free; model generation gated/reserved | Wrong recipient/company, revoked evidence, missing contact, duplicate create | Draft may exist without send permission; every fact backed by selected evidence; TEST-BO-021-01; BO-021 |
| REQ-DRAFT / UI-26 | Generate/edit draft, objective/tone/language/value/followup; `editDraft` and inline generator<br>Fixed generic template; subject HarbourSense; evidence panel always first source; each edit resets approval | SOURCE_VERIFIED; browser subset above | `generateDraft`, `editDraft`, `getAsyncJob` | Immutable DraftRevision + evidence bindings | Content editor; purpose-approved data only | Model reservation/actual token accounting | Unsupported claims, prompt injection, concurrent editors, provider timeout | Uncited claims flagged; revision increments; evidence and exact recipient bound; TEST-BO-021-01; BO-021 |
| REQ-APPROVAL / UI-27 | Request review / approve; inline Workspace handlers<br>Browser status; sender/opt-out/valid recipient gates; hardcoded Willy actor; approval revision only | SOURCE_VERIFIED; browser subset above | `requestDraftReview`, `approveDraft` | ReviewRequest; Approval exact content+recipient+evidence+policy+sender hash | Reviewer role; outreach policy decision independent of research | Free | Stale approval, content/context changed concurrently, suppression during approval | Transaction rejects stale digest; evidence/policy/recipient changes invalidate approval; TEST-BO-022-01; BO-022 |
| REQ-EXPORT / UI-28 | Save/copy/download draft; save-draft modal, clipboard, `download`<br>Local .example-only save; reload resets to Draft; no sender delivery | SOURCE_VERIFIED; browser execution NOT RUN | `editDraft`, `exportDraft`, `downloadExport` | Draft persistence, export event | View/edit/export permissions | Free | Storage quota/invalid JSON, clipboard denial, edited recipient, accidental approval reuse | Live text never put in demo localStorage; reload revalidates context; no delivery status change; TEST-BO-021-01; BO-021, BO-023 |
| REQ-DELIVERY / UI-29 | Send email; disabled button<br>Disabled; delivery enum only Not connected | SOURCE_VERIFIED; browser subset above | `disabledDeliveryBoundary` (always denied in MVP-A) | Delivery separate from approval, remains not_connected | Later explicit delivery-phase approval | No delivery cost in pilot | Crafted direct send request, stale approved draft | UI and API have no enabled send/schedule path; approved ≠ sent; TEST-BO-030-01; BO-030 |
| REQ-OUTCOME / UI-30 | Record outcome; Results inline `setS`<br>Single latest manual stage per buyer, replaces previous; placeholder also selectable | SOURCE_VERIFIED; browser execution NOT RUN | `recordOutcome`, `correctOutcome` | OutcomeEvent append-only, manual origin; projection latest stage | Member recorder; source provenance | Free | Duplicate event, correction, time ordering, ambiguous placeholder | Manual event cannot fabricate email send/reply ingestion; prior stages retained; TEST-BO-024-01; BO-024 |
| REQ-USAGE / UI-31 | Usage and overview metrics; `usage`, run-summary, Results<br>Client sums float ledger; distinct accepted/valid companies; expired Reserved quote removed | SOURCE_VERIFIED; browser subset above | `getUsage`, `listBudgets` | BudgetAccount, Reservation, CostEvent, denominators | Budget reader/admin distinction | Reports all actual and pending cost | Zero denominator, mixed currency, unknown settlements, late vendor invoices | Stable scoped denominators, unknown hold shown, no zero-cost claim after provider timeout; TEST-BO-024-01; BO-024 |
| REQ-UI / UI-32 | Settings budgets, locale, local save/reset, connections<br>User edits all budgets; local save/redaction; language dictionary; requirements-only dialogs | SOURCE_VERIFIED; browser subset above | `updateBudget`, `getPreferences`, `updatePreferences`, `getCapabilities` | WorkspaceSettings, UserPreferences, capability manifest | Budget admin; user locale; policy admin | Limits govern subsequent paid operations | Budget decrease beneath holds, offline write, unsupported locale, no provider configured | Cap cannot invalidate existing liabilities; connection state reflects health/evidence, never banner removal; TEST-BO-009-01; BO-009, BO-010, BO-024, BO-025 |

## Migration checklist and present gaps

The complete [source migration checklist](research/SOURCE_AUDIT.md#6-concrete-gaps-and-state-migration-checklist) enumerates every current fixture loader, adapter, store, timer, browser key, direct mutation and important metric. It is part of this audit. BO-006 moves seed/timers/storage to isolated demo mode; BO-007/008/016 replace current local writes with scoped API state; BO-017–020 replace browser quote liabilities; BO-021/022 bind full draft context. All new module paths belong in selected task `proposed_files_to_create`, labelled PROPOSED.

Highest-priority source defects for live integration: (1) unknown run ID falls back to current sample; (2) all runs share mutable global companies/evidence; (3) contact suppression overwrites validity and removal loses history; (4) expired quote holds disappear without any provider liability model; (5) profile confirmation/approval is browser-only; (6) draft generation uses generic fixed facts and the first evidence record; (7) old/recent filters do not compare actual timestamps; (8) no live actor, membership or audited policy; (9) root layout ignores page children; (10) filter/draft/tab state is not durable URL state. Preserve the visual flow, fix these behaviors.

Preserve the three complete fictional dossiers, 26→24 mappings, 14/6/4 buckets, mixed review/contact states, in-app sample source panels, draft approval invalidation, list membership semantics, demo disclosures, formula-safe demo CSV and English/zh-HK dictionary structure. A fixture count is not proof of live canonicalization; browser controls are not security boundaries.

## Baseline commands and checks

- RUN: `git status --short`, `git rev-parse HEAD`, `git rev-parse HEAD^{tree}`, remote and tracked-source reads; exact GitHub branch tree and Sites version comparisons.
- RUN: package/lock/deployment/schema/instruction/test source reads; full available master and earlier frontend spec reads.
- NOT RUN: `pnpm exec tsc --noEmit`, `pnpm run lint`, `pnpm run build`, `node tests/domain-checks.mjs`. Commands/targets exist in inspected source; node_modules absent; no install/hooks allowed by this planning work.
- NOT RUN: API/DB/queue/provider/concurrency/Playwright suites; new services/test files do not yet exist.
- NOT RUN: installed OpenCode version; executable absent on PATH. No active configuration created.
- NOT RUN: real provider smoke tests, mailbox integration, sends, cloud provisioning, database migrations and deployment. No live-backend capability is verified.
